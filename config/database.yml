development:
  adapter: mysql2
  encoding: utf8
  reconnect: false
  database: br_development
  pool: 5
  username: <%= ENV['DB_USERNAME'] || 'root' %>
  password: <%= ENV['DB_PASSWORD'] || '' %>

# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.
test:
  adapter: mysql2
  encoding: utf8
  reconnect: false
  database: timeline_genius_test
  pool: 5
  username: <%= ENV['DB_USERNAME'] || 'root' %>
  password: <%= ENV['DB_PASSWORD'] || '' %>

production:
  adapter: mysql2
  encoding: utf8
  reconnect: false
  database: br_production
  pool: 5
  username: root
  password: qeHGIFxsTH
